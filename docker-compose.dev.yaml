services:
  nextjs:
    depends_on:
      - django
    build:
      args:
        - BUILD_ENV=dev
    environment:
      - WATCHPACK_POLLING=true

  django:
    build:
      args:
        - BUILD_ENV=dev
    volumes:
      - ./django:/app

  nginx:
    build:
      args:
        - BUILD_ENV=dev
